<h1>Listado de items</h1>

<div style="margin: 1rem;">

    <h2> {{ item.title }}</h2>
    <p> {{ item.id }}</p>
    <p> {{ item.created }}</p>

    {% for item in items %}
        <div>
            <a href="/listar/{{ item.id }}"> Ver artículo {{ item.id }} </a>
            <br/>
        </div>
    {% endfor %}
</div>


